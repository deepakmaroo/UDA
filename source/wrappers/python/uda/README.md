# DEPRECATION WARNING

This package is changing name to [pyuda](https://pypi.org/project/pyuda/). please install from there instead.

For now: pip installing this package will simply pull the new repository name as a dependency and install a dummy package which will warn you to import pyuda instead at runtime. 

Note that this name will be fully deprecated from uda version 3.0 release onwards and this repository will not be available for use at all. 
